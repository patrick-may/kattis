#include <string>
#include <algorithm>
#include <iostream>
#include <set>

using namespace std;
int main() {
    string word;
    cin >> word;
    set<string> rots;
    string spin = 
    while (next_permutation(word.begin(), word.end() != word) {
            rots.push_back(word)
            }
    return 0;
}
